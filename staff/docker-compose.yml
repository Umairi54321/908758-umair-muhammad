version: "3.8"

services:
  staff:
    build: .
    ports:
      - "8002:80"
    volumes:
      - .:/var/www/html
    restart: unless-stopped
    networks:
      - app-network

networks:
  app-network:
    external: true
