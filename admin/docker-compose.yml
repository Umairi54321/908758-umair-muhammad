version: "3.8"

services:
  admin:
    build: .
    ports:
      - "8001:80"
    volumes:
      - .:/var/www/html
    restart: unless-stopped
    networks:
      - app-network

networks:
  app-network:
    external: true
