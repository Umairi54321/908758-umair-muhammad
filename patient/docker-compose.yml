version: "3.8"

services:
  patient:
    build: .
    ports:
      - "8003:80"
    volumes:
      - .:/var/www/html
    restart: unless-stopped
    networks:
      - app-network

networks:
  app-network:
    external: true
